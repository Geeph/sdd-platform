#!/bin/sh
# Gradle wrapper stub (placeholder — real wrapper is generated by `gradle wrapper`).
# This stub exists so the template source tree is self-describing; before running
# any Gradle command the consumer must regenerate a real wrapper with:
#   gradle wrapper --gradle-version 8.10
echo "error: gradle wrapper not initialised — run 'gradle wrapper --gradle-version 8.10'" >&2
exit 1
