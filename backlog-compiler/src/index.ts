/**
 * @sdd/backlog-compiler — placeholder package.
 *
 * Real implementation lands in M5 (strategies + dependency reconciliation +
 * Issue upsert). This file exists so that workspace consumers can resolve the
 * package in M1 without importing missing code.
 */
export const M1_PLACEHOLDER = '@sdd/backlog-compiler';
