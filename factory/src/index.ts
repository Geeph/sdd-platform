/**
 * @sdd/factory — placeholder package.
 *
 * Real implementation lands in M2 (product init, scaffold). This file exists
 * so that workspace consumers can resolve the package in M1 without importing
 * missing code.
 */
export const M1_PLACEHOLDER = '@sdd/factory';
